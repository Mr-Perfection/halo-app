mutation integrationsDBCredentials($type: DBType!, $connectionString: String!) {
  createDBCredentials(type: $type, connectionString: $connectionString) {
    id
    connectionString
    type
    createdAt
    updatedAt
  }
}
